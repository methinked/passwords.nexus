services:
  passwds-web:
    image: nginx:alpine
    container_name: passwds-web
    restart: always
    ports:
      - "8091:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
